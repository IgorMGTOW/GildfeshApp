#include <stdio.h>
#include <stdlib.h>

int main()
{
    int Gildfesh1 = 1;
    void *p = calloc((size_t) 10, (size_t) 99999);
    printf("Welcome to Gildfesh test application!\n");
    while (Gildfesh1 =1) {
        printf("Gildfesh is a member of the staff team in Save Minecraft Server");
    }
    free(p);
    return 0;
}
